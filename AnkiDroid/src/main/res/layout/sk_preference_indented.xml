<?xml version="1.0" encoding="utf-8"?>
<!-- Fork: kxkb-style item row for the 白い熊 暗記 UI page — indented one level
     under its section heading, tight vertical padding (minHeight 0 kills the
     androidx 48dp minimum). Contains the standard preference ids so androidx
     Preference binds title/summary/icon and the widget frame normally; the
     icon (our colour swatch) sits kxkb-style on the RIGHT of the row. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:minHeight="0dp"
    android:paddingStart="72dp"
    android:paddingEnd="16dp"
    android:paddingTop="5dp"
    android:paddingBottom="5dp"
    android:background="?android:attr/selectableItemBackground">
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">
        <TextView
            android:id="@android:id/title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#FFFFFF00"
            android:textSize="16sp"/>
        <TextView
            android:id="@android:id/summary"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#FF9E9E9E"
            android:textSize="13sp"/>
    </LinearLayout>
    <ImageView
        android:id="@android:id/icon"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_marginStart="12dp"
        android:contentDescription="@null"/>
    <LinearLayout
        android:id="@android:id/widget_frame"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"/>
</LinearLayout>
